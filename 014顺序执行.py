# Python代码在执行过程中，遵循下面的基本原则：
"""  
1、Python代码在执行过程中，遵循下面的基本原则：

2、碰到函数，将函数体载入内存，并不直接执行
3、碰到类，执行类内部的普通语句，但是类的方法只载入，不执行
4、碰到if、for等控制语句，按相应控制流程执行
5、碰到@，break，continue等，按规定语法执行
6、碰到函数、方法调用等，转而执行函数内部代码，执行完毕继续执行原有顺序代码


"""